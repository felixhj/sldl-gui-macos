# SoulseekDownloader Python Dependencies
# Requires PyObjC for the Cocoa GUI implementation

# Core PyObjC frameworks for macOS GUI
pyobjc-framework-Cocoa>=9.0
pyobjc-core>=9.0

# Note: The 'sldl' command-line tool from slsk-batchdl is automatically
# bundled with this application during the build process.
